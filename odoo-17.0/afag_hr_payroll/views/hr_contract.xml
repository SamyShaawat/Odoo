<odoo>
    <record id="hr_contract_view_form" model="ir.ui.view">
        <field name="name">View Contract Form</field>
        <field name="model">hr.contract</field>
        <field name="inherit_id" ref="hr_contract.hr_contract_view_form"/>
        <field name="arch" type="xml">
            <group name="salary" position="inside">
                <label for="total_wage" invisible="l10n_sa_company_country_code != 'SA'" string="Total Wage"/>
                <div class="o_row" name="total_wage" invisible="l10n_sa_company_country_code != 'SA'">
                    <field name="total_wage" nolabel="1"/>
                    <span>/ month</span>
                </div>
            </group>
        </field>
    </record>
</odoo>